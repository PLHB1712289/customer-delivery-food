import { Grid } from "@material-ui/core";
import React from "react";
import "./styles.css";

const ListItems = ({ lable, onClickMore, children }) => {
  return <Grid className="ListItems">ListItems</Grid>;
};

export default ListItems;
